{
  "name": "teamsbot",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "targets": {
    "container": {
      "executor": "@nx-tools/nx-container:build",
      "defaultConfiguration": "local",
      "options": {
        "engine": "docker",
        "file": "services/teamsbot/Dockerfile",
        "platforms": ["linux/amd64"]
      },
      "configurations": {
        "local": {
          "tags": ["teamsbot:latest"],
          "push": false
        },
        "production": {
          "push": true,
          "tags": ["aster/teamsbot:latest"]
        }
      }
    }
  }
}
