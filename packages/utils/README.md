# utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build @aster/db` to build the library.

## Running unit tests

Run `nx test @aster/db` to execute the unit tests via [Jest](https://jestjs.io).
